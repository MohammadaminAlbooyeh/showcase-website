// src/pages/Resume.jsx

import React from 'react';
import './Resume.css'; // Import the CSS file for Resume styles

export default function Resume() {
  return (
    <div className="resume-page">
      <h1>My Resume</h1>
      <div className="resume-box">
        {/* This box is currently empty as requested. */}
        {/* You can add your resume content here later, e.g., text, lists, or a PDF embed. */}
        {/* For example: <p>Your resume content will go here.</p> */}
      </div>
    </div>
  );
}
